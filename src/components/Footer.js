/** @jsxImportSource @emotion/react */
import { css, useTheme } from "@emotion/react";

function Footer() {
    const gallery = css`
        background: yellow;
    `;

    return (
        <div  css={gallery}>
            hello
        </div>
    );
}

export default Footer;